{
  "name": "latido",
  "version": "0.4.2",
  "private": true,
  "description": "Latido — a rhythm-driven UI engine for the web",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "npm --workspace examples/basic run dev",
    "build": "npm --workspaces --if-present run build"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
